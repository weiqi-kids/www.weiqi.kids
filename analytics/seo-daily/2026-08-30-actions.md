# 2026-08-30 執行記錄

## ① 昨日賭注驗證

昨日（08-29）commit `5d5214018`：英文版 PUCT 公式頁（`en/docs/alphago/puct-formula`，全站曝光最大單頁）補上 WebPage/Article/FAQPage/KeyTakeaway schema，未動標題與描述文案，賭的是讓 Google 更完整索引該頁內容以撐住既有排名／逐步墊高 CTR。

- 該頁 deploy 於 08-29 18:54，距今日數據不到 24 小時，GSC 有 2-3 天延遲，**首訊號日未到，不判勝負**。
- 對照數字：query「puct」impressions 21→33、position 4.9→5.4（皆在正常波動範圍內），clicks 仍 0。
- 判定：**還看不出來**，明日/後續繼續追蹤，尚不需回退。

補記：昨日（08-29）實際跑了內容修改並 push，但當時未產出/提交 `2026-08-29-actions.md`（`git log` 只查到內容 commit，無對應執行記錄 commit），流程有缺口，本篇一併補上事後說明供明日銜接參考。

## ② 今日訊號判讀摘要

- `gsc.channelHealth` 的 `cliff` 今日四個分組（含唯一有效訊號的「英文」）全部為空清單，無懸崖帶候選可挑。
- `strikingDistance` / `highImpZeroClick` 集中在兩頁：
  - `en/docs/alphago/puct-formula`（query「puct」，imp 33，pos 5.4）→ 昨日剛動過，站規 7 天內同頁不重複動標題/結構，跳過。
  - `docs/about/members/founding/feng-hao-chiang`（query「姜封豪」，imp 28，pos 9.4，clicks 仍 0）→ 此頁近期已連續 4 輪處理過（07-03 description、07-08 補經歷、07-09 頁首量化摘要、08-19 description 再調），每次都未能拉出點擊；同時出現「姜封豪診所」「姜封豪 診所的評論」等搜尋意圖明顯是想找診所評價而非協會會員頁，非標題/描述可解的落差。今日不再重複同類編輯，避免無效編修。
- `index.coverage` 中 `docs/learn/`（旗艦頁、學圍棋分類首頁）仍是「Discovered - currently not indexed」，且全站 `docs/**` 內容中只有 1 個檔案內鏈指向它（`docs/about/internal/misc/index.md`）。依 SOP 第 4 步「重點頁未收錄 → 強化內鏈入口」，今日改走內鏈補強而非動事實內容。

## ③ 今日實際改動

不涉及任何事實型內容，純導覽內鏈補強：

1. `docs/alphago/index.md` — 「延伸學習」清單新增一條 `[學圍棋](/docs/learn/) - 想真正下一盤棋？從規則開始學起`，把全站流量最大主題頁之一（AlphaGo 系列）導向尚未收錄的 `docs/learn`。
2. `docs/tech/index.md` — 「預備知識」表格中「圍棋規則」列，把原本未加連結的舊名稱「給棋友」（改版後已不存在此分類名）改成指向現行 `docs/learn` 的正式連結 `[學圍棋](/docs/learn/)`，順手修掉一個過時的分類名稱殘留。

賭的是：兩個高權重旗艦頁（`docs/alphago`、`docs/tech`）新增內鏈能增加 `docs/learn` 的爬取入口與內部權重傳遞，幫助它從「Discovered - currently not indexed」轉為已收錄；一旦收錄，也連帶解除 08-03 拍板「`docs/learn` 未收錄前不做圍棋學習資源策展頁」的前提之一。

## 技術細節

- 改動檔案：`docs/alphago/index.md`、`docs/tech/index.md`（2 檔，未達 5 檔上限）
- `check-zh-hant.mjs` 檢查：通過，無簡體字
- `pnpm build`：全 11 語系建置成功；`onBrokenLinks`/`onBrokenMarkdownLinks` 均為 `throw` 且建置無報錯
- commit：`[auto-claude-seo] 2026-08-30: docs/learn 內鏈補強（alphago、tech 首頁導流）`
- push 後執行 `analytics/scripts/deploy.sh` 部署，並對本次改動的 2 個網址呼叫 IndexNow
