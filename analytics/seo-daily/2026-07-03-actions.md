# SEO 每日行動記錄 — 2026-07-03

## ① 昨日賭注驗證

無：`analytics/seo-daily/` 底下目前只有 2026-07-01 / 07 / 02 / 03 四份 JSON，且過去皆無 `-actions.md`，判斷是本自動化流程首次執行，無前一日賭注可驗證。

## ② 今日訊號判讀

- 讀取 `analytics/seo-daily/2026-07-03.json`（GSC range 2026-06-22..2026-06-29，GA4 range 7daysAgo..yesterday）。
- **重要發現**：GSC 是 domain-wide property（`sc-domain:weiqi.kids`），`topQueries`/`pageQueryCross`/`strikingDistance`/`highImpZeroClick` 混入大量與 www.weiqi.kids 完全無關的子網域資料（epialert.weiqi.kids 疾病警示、learn.weiqi.kids 考試門檻、proof.weiqi.kids 橢圓曲線數學、ecommerce/trade/risk/skills 等）。今日判讀時先過濾只留 `www.weiqi.kids` 網域本身的列，再考慮行動。
- **過濾後與 www.weiqi.kids 相關的訊號**：
  - `docs/about/members/founding/feng-hao-chiang/`（姜封豪，創始會員頁）同時出現在 strikingDistance 與 highImpZeroClick：
    - zh-tw：「姜封豪」pos 5、imp 3、0 click；「姜封豪診所」pos 12.67、imp 3、0 click；「姜封豪醫師學歷」pos 6.67、imp 3、0 click
    - en：「姜封豪醫師學歷」pos 5.5、imp 4、0 click；「姜封豪」pos 14.5、imp 2、0 click
  - `docs/tech/how-it-works/katago-innovations/`：「tromp taylor rules」pos 8、imp 1 —— 訊號太弱（單次曝光），今日不動。
  - `en/docs/about/members/founding/ian-kuo/`「ian kuo」、`en/docs/about/members/founding/jing-yan-chou/`「chou jing」：pos 8、imp 1 —— 同樣訊號太弱，不動。
  - 首頁「weiqi」imp 4、pos 41.25、0 click —— 排名太後面（41），非 strikingDistance 範圍，非今日可行動項。
- **index.coverage 澄清**：JSON 裡列出的 `/for-players`、`/for-engineers`、`/evolution`、`/aboutus`「URL is unknown to Google」是**舊網站結構**（commit `fef93e75` reorganize 之前）的路徑，現在網站已改為 `docs/about`、`docs/alphago`、`docs/animations`、`docs/learn`、`docs/tech` 分類，這些舊路徑本來就不存在，Google 顯示「unknown」正常、不是待修問題。`en/`「Duplicate, Google chose different canonical」與 `ja/`「Page with redirect」經即時 curl 檢查，兩頁目前 canonical/hreflang 標籤皆正確（200、canonical 指向自身），研判是舊爬取快照，等 Google 重新抓取即可，非程式碼問題，今日不動。
  - （備註：cron 的站台脈絡文件把 for-players/for-engineers/evolution/aboutus 列為「旗艦頁」已過時，建議下次找機會更新該份文件，但今日流程規定只能改 ≤5 檔且聚焦事實查證，不在今日範圍內處理文件本身。）

## ③ 今日改動

**改了 1 檔**（`docs/about/members/founding/feng-hao-chiang.md`）：

- **問題**：頁面 meta description 原為 `運動通健康`（純標語 4 字），不含姓名、職稱、專業領域，導致 Google 對「姜封豪」「姜封豪診所」「姜封豪醫師學歷」三個查詢字詞排名落在 5–14.5 名（頗前面）卻完全零點擊——使用者在搜尋結果看不到有效資訊去判斷這是不是他們要找的頁面。
- **做法**：改寫 description 為「姜封豪為健康領域聯合診所院長，專精運動營養家庭醫學，具13年專業實務經驗，是台灣好棋寶寶協會創始會員。」——所有資訊均取自頁面本文既有內容（`## 專業領域`、`## 經歷`、H1 姓名），未新增任何頁面未記載的事實（未使用「醫師」頭銜，因頁面本身未明確記載其醫事執照，避免臆測敏感職稱）。此為 zh-tw 原始檔改動，未動翻譯檔（i18n/**）。
- **驗證**：`pnpm install --frozen-lockfile` → `pnpm build` 全部 11 語系皆 SUCCESS；build 產物內 `build/docs/about/members/founding/feng-hao-chiang/index.html` 的 `<meta name="description">` 已確認更新為新文案。
- **上線**：
  - commit `5fee2027`「[auto-claude-seo] 2026-07-03: 姜封豪頁面 meta description 補實質內容以貼合搜尋詞」→ `git pull --rebase`（無衝突）→ `git push origin main`（`bceca4f7..5fee2027`）。
  - `bash analytics/scripts/deploy.sh`：`pnpm build` 成功 → 強推 `build/` 到 `gh-pages`（`ffca828...32f9005`）→ `[deploy] ✅ 已發佈到 gh-pages`。
  - 線上驗證：curl 該頁時 CDN 仍回舊快取（`x-cache: HIT`、`cache-control: max-age=600`），屬正常傳播延遲（10 分鐘內會過期更新），非部署失敗。
  - `node analytics/scripts/indexnow-ping.mjs https://www.weiqi.kids/docs/about/members/founding/feng-hao-chiang/` → `IndexNow → 200 OK`。
- **預期效果**：3 個查詢字詞的點擊率提升（目前 0 click），進而可能推升平均排名；下次驗證這 3 個查詢字詞在下一份 GSC 資料（約 2–3 天後）的 clicks/position 是否改善。

## 今日檔案清單（共 1 檔，未超過每日上限 5 檔）

- `docs/about/members/founding/feng-hao-chiang.md`（zh-tw 原始檔，meta description 改寫）
