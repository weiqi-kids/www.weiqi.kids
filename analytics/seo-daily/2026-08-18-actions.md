# 2026-08-18 SEO/AEO/GEO 執行記錄

## ① 昨日賭注勝負
無（前一日 `2026-08-17-actions.md` 不存在，`scoreboard.json` 尚未建立，本輪無賭注可對帳）。

## ② 今日判讀摘要
- GSC 範圍：2026-08-07..2026-08-14，`sc-domain:weiqi.kids` 為 domain-wide property，已依站規只採信 `www.weiqi.kids` 路徑的列。
- `targetTrend` 顯示 `puct`／`puct formula` 兩個 pinned query 週對週排名明顯進步（puct: 6.5→4.6；puct formula: 6.29→4.5），曝光 10-14 次，但兩者 CTR 皆為 0%，同時出現在 `highImpZeroClick` 與 `strikingDistance`——判讀為「排名已在進步但 meta 未跟上」的真實機會，非雜訊。
- 對應頁面 `docs/alphago/15-puct-formula.mdx`（繁中原始，已有完整 ArticleSchema/FAQ/KeyTakeaway）在 EN 語系的 `i18n/en/.../15-puct-formula.mdx` frontmatter title/description 明顯偏泛用（"PUCT Formula Explained" + 破折號定義句），未針對查詢意圖優化。
- `docs/about/members/founding/feng-hao-chiang/` 相關人物查詢（姜封豪、姜封豪醫師學歷）雖也在 strikingDistance/highImpZeroClick，但曝光基數僅 1-5 次、樣本過小，且涉及真人職稱敏感資訊，本輪不動，留待樣本量提升。
- 其餘 highImpZeroClick/strikingDistance 項目多為 i18n 翻譯頁（ja/hi/ko/en 的創始會員頁），依站規本輪不動翻譯內容頁。
- `index.coverage`：`docs/learn/` 仍是 Discovered-currently-not-indexed；`docs/tech/`、`docs/alphago/` 為 redirect（目錄頁重導向子頁，正常）。sitemap 警告持續為 0（非 3 個，優於代管拍板時的狀態，不用理會)。

## ③ 本次改動
- 檔案：`i18n/en/docusaurus-plugin-content-docs/current/alphago/15-puct-formula.mdx`
  - 依站規「單一語系 meta/frontmatter 修正」例外（不計入 docs/** 原則、仍計入每日 ≤5 檔上限，本次僅用 1 檔）。
  - `title`: `PUCT Formula Explained` → `PUCT Formula: AlphaGo's MCTS Selection Rule Explained`（53 字元，關鍵字「PUCT Formula」前置＋加入差異化承諾）
  - `description`: 移除破折號定義句式，改寫為具體涵蓋公式推導／Python 程式碼／各專案 c_puct 調參值（145 字元，≤155）
  - 僅動 frontmatter，未動內文/程式碼/FAQ/Schema。
- commit：`44c41d2c5`（main），已 push；`analytics/scripts/deploy.sh` 成功，已強推 `41d081d` 到 `gh-pages`。
- 已驗證：`git diff` 只改 2 行 frontmatter；`pnpm build` 11 語系全過（含 onBrokenLinks/onBrokenMarkdownLinks throw 守門）；直接讀 gh-pages raw HTML 確認新 title/description 已生效；線上 www 因 CDN cache-control max-age=600 仍短暫顯示舊值，屬正常傳播延遲，非部署失敗。
- 已呼叫 `indexnow-ping.mjs` 通知 `https://www.weiqi.kids/en/docs/alphago/puct-formula/`（200 OK）。

## 賭注
- 目標：`puct formula` / `puct` 兩詞在 `/en/docs/alphago/puct-formula/` 的 CTR，從目前 0%（12-14 次曝光、0 點擊）轉為 >0%，且排名維持在 4-6 名區間或持續進步。
- 預期首訊時間：約 4 天後（2026-08-22 前後，GSC 資料延遲 2-3 天）。
- `scoreboard.json` 尚未建立，本輪未寫入結構化帳本；下次若該檔存在會恢復使用。

## Slack
已發送，🟢（系統自己處理好了，無需用戶決定）。
