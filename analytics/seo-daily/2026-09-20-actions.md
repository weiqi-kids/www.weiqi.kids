# 2026-09-20 SEO 執行紀錄

## 1. 昨日賭注驗證
- 昨天（2026-09-19）賭注：`docs/about/members/founding/zi-yan-huang.md` 補 key-answer，賭「黃子彥」查詢 CTR/position 改善。
- 今日 GSC 資料區間（2026-09-09..2026-09-16）尚未涵蓋 09-19 部署後的流量（GSC 有 2-3 日延遲），「黃子彥」仍是 4 曝光、position 9.83、CTR 0，與昨日相同——資料區間未動，屬正常「還看不出來」，非退步。

## 2. 今日判讀摘要
- `gsc.channelHealth` 各分組 `cliff` 皆為空（含「英文」分組），本次無懸崖帶訊號，改用 `highImpZeroClick`。
- `highImpZeroClick` 候選逐一檢視：
  - `docs/about/members/founding/feng-hao-chiang/`（姜封豪，23 曝光、CTR 0、position 9.6）：已三度優化（07-09、08-19、09-07）且已有頁首量化摘要，不符合再優化條件，跳過。
  - `en/docs/alphago/puct-formula/`（多個 puct 相關查詢）：title/description 已完整對齊查詢意圖（含 AlphaGo、MCTS、c_puct），dateModified 2026-08-29，判斷已充分優化，且屬事實/技術密度高頁面，非首選。
  - `docs/about/members/founding/zi-yan-huang/`：昨天才改過，7 天內冷卻中，跳過。
  - `docs/about/members/founding/tien-chien-pan/`（潘天健，查詢「潘天健」4 曝光、position 13.75，同時列在 strikingDistance）：`## 關於` 段落只有敘述性文字，無 `.key-answer` 量化摘要，且從未被本流程動過（git log 確認），符合「創始會員頁量化摘要置頂」既有模式，本次挑選此頁。

## 3. 本次改動
- 檔案：`docs/about/members/founding/tien-chien-pan.md`（僅 1 檔，繁中原始檔）
- 改動：在「## 關於」段落開頭插入 `<p className="key-answer" data-question="潘天健醫師的資歷是什麼？">`，內容完全取自頁面既有「現任職務」「學歷」「專業認證」段落既有事實（沁安整合醫學診所院長、台灣細胞分子矯正學會理事長、中國醫藥大學中醫學系中西雙主修＋碩士、中醫師與西醫內科專科醫師雙證照），未新增未查證事實，無需 WebSearch。
- zh-hant 機械檢查：`check-zh-hant.mjs` 通過，無簡體字。
- 賭注：查詢「潘天健」（目前 4 曝光、position 13.75、CTR 0）與該頁在 strikingDistance／highImpZeroClick 名單，預期加入量化 key-answer 後標題/內文更貼搜尋意圖，position 有機會前移、CTR 有機會由 0 出現點擊。
- Gate：`pnpm install --frozen-lockfile` → `pnpm build`（11 語系全數成功）→ `onBrokenLinks`/`onBrokenMarkdownLinks` 均為 `throw`，全數通過。
- Git：commit `f8e510280`（[auto-claude-seo] 2026-09-20），`git pull --rebase origin main` 前先 `stash push -u` 暫存本次無關的既有未提交異動（revamp/ 系列文件、CONTEXT.md、docs/adr/ 等），rebase 乾淨（up to date）後 `stash pop` 還原，push 成功。
- 部署：`analytics/scripts/deploy.sh` 成功，`gh-pages` 已強推新版；透過 `raw.githubusercontent.com` 確認 gh-pages 上的 HTML 已含新 key-answer 段落。前台 CDN（`www.weiqi.kids`，cache-control max-age=600）當下仍回舊版快取（age 31s、x-cache HIT），屬正常 CDN 傳遞延遲（10 分鐘內會過期更新），非部署失敗。
- IndexNow：已推送 `https://www.weiqi.kids/docs/about/members/founding/tien-chien-pan/`，200 OK。

## 備註
- 工作目錄仍有與本流程無關的既有未提交異動（`revamp/0-positioning/positioning.md`、`revamp/1-discovery/discovery.md`、`revamp/2-competitive/competitive.md`、`revamp/3-analysis/analysis.md`、`revamp/4-strategy/strategy.md`、`revamp/5-content-spec/content-spec.md`，及多個 `revamp/` 下未追蹤檔、`CONTEXT.md`、`docs/adr/`），研判為其他任務留下的工作中內容，本次未動、未納入 commit，僅在 `git pull --rebase` 前後 `stash push -u` / `stash pop` 以避免衝突，內容已還原完整（git stash pop 後 diff 與 push 前一致）。
- 昨日筆記提到 `en/docs/alphago/puct-formula/` 曾是本站最高流量頁之一，今日訊號顯示其 title/description 已對齊查詢意圖，暫不需再動；後續若該頁再度出現 highImpZeroClick 且既有優化已過 7 天冷卻，可評估是否需要更細緻的 answer-first 段落調整（屬技術密度高頁面，任何事實性補強仍需先過 WebSearch 查證）。
