# 2026-08-20 SEO/AEO/GEO 執行記錄

## ① 昨日賭注勝負
- 賭注 A（2026-08-18 下注）：`/en/docs/alphago/puct-formula/` 的 `puct`／`puct formula` 從 0% CTR 轉為 >0%，預期首訊約 2026-08-22。今日兩詞仍 0 clicks（`puct` 15 曝光、`puct formula` 12 曝光，position 反而從約 6→4.4／4 進步）。**還看不出來**，首訊未到（還差 2 天），非退步，不判定。
- 賭注 B（2026-08-19 下注）：`docs/about/members/founding/feng-hao-chiang/`（姜封豪）「姜封豪醫師學歷」「姜封豪醫師」轉為 >0% CTR，預期首訊約 2026-08-23。今日「姜封豪醫師學歷」排名進步到第 2.2 名（5 曝光）但仍 0 點擊；「姜封豪醫師」3 曝光、排名 11.3、仍 0 點擊。**還看不出來**，首訊未到（還差 3 天），非退步，不判定；排名走勢正向。
- `analytics/seo-daily/scoreboard.json` 目前不存在，本輪仍用散文法追蹤，不建立結構化帳本。

## ② 今日判讀摘要
- GSC range：`sc-domain:weiqi.kids` 為 domain-wide property，已只採信 `www.weiqi.kids` 路徑的列（`groupTotals.英文` 1058 曝光/1 點擊為最大占比，其餘分組皆 0；domainWide 對照組 10 clicks/3634 impr，本站以外流量不計入判讀）。
- `strikingDistance` ∩ `highImpZeroClick` 交集中新出現 `docs/about/members/founding/zi-yan-huang/`（黃子彥，繁中原始頁）：查詢「黃子彥」排名 10.75、4 次曝光、0 點擊。檢查該頁 frontmatter 發現 `description` 只有「漢方保健品」四字，**完全沒有姓名與職稱**，是本次交集中訊號最明確的機會（description 缺失比排名/曝光問題更根本）。
- 姜封豪頁（feng-hao-chiang）與 puct-formula 頁本輪不重複觸碰，皆在昨日/前日下注觀察窗內，尚未到首訊日，等窗口到再判定，避免同頁連續改動干擾歸因。
- 其餘 highImpZeroClick 項目（`林昊德`／`謝依純`／`ian kuo`／`顏怡璇`等）皆落在 i18n 翻譯頁（ja/en/pt），依站規本輪原則不動翻譯內容頁。
- `index.coverage`：`docs/learn/` 仍為「URL is unknown to Google」（未達重啟「圍棋學習資源」策展頁的兩個前提條件，不提案）；sitemap-index.xml 警告已降為 0（優於代管拍板時的 3 個警告基準，早前已結案不追蹤，這裡僅記錄一次確認即可，之後不再逐日記錄）。
- `geo`／条件式 schema 檢查：本頁改動只動 `description` 一行，未動 `title`／H1／內文事實，不影響既有 7 種必填 JSON-LD 與 SGE 標記結構。

## ③ 本次改動
- 檔案：`docs/about/members/founding/zi-yan-huang.md`（繁中原始檔，計入每日 ≤5 檔上限，本次僅用 1 檔）
  - 僅改 `description` frontmatter 一行，未動 `title`（維持「純姓名」慣例）、未動內文/經歷/合作平台/聯繫方式等既有事實。
  - 舊：`漢方保健品`
  - 新：`黃子彥現任GCM上醫預防醫學發展協會數位編輯部總編輯，長期投入預防醫學與中醫養生知識整合，是台灣好棋寶寶協會創始會員。`
  - 新增用詞（總編輯／預防醫學／中醫養生／協會創始會員）皆已存在於頁面本文（「經歷」「關於」段落），僅重組既有事實貼近查詢意圖（查詢詞「黃子彥」），未查證新事實、未新增未經查證資訊。
- commit：`b54c0f0eb`（main），已 push（`1d395de22..b54c0f0eb`）。
- 驗證：`node check-zh-hant.mjs` 通過（無簡體字）；`pnpm build` 11 語系全過（含 onBrokenLinks/onBrokenMarkdownLinks throw 守門）；`analytics/scripts/deploy.sh` 成功強推至 `gh-pages`（`907bd30`）；本機建置產物 `build/docs/about/members/founding/zi-yan-huang/index.html` 已確認新 description 寫入（含 Person schema 的 description 欄位同步更新）；線上頁 CDN 快取尚未刷新（仍讀到舊值），屬正常傳播延遲，非部署失敗。
- 已呼叫 `indexnow-ping.mjs` 通知 `https://www.weiqi.kids/docs/about/members/founding/zi-yan-huang/`（200 OK）。

## 賭注
- 目標：「黃子彥」查詢（目前排名 10.75、4 次曝光、0 點擊）轉為 >0% CTR，排名維持或進步。
- 預期首訊時間：約 4 天後（2026-08-24 前後，GSC 資料延遲 2-3 天）。

## Slack
已發送。
